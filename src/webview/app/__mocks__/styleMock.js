// Stylesheet stub for jest. The webview bundle imports app.css so esbuild
// emits dist/webview.css; under test there is nothing to load and nothing to
// assert, so the import resolves to an empty object.
module.exports = {};
