---
phase: 05-webview-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/webview/app/components/TrendsTab.tsx
  - src/webview/app/components/UsageChart.tsx
  - src/webview/app/components/SegmentedControl.tsx
autonomous: true

must_haves:
  truths:
    - "Stacked bar chart renders with four token types as stacked bars"
    - "User can switch between daily, weekly, and monthly views via segmented control"
    - "Tooltip appears on hover showing per-type values and total"
    - "Data table expands to show exact numbers per period"
    - "Chart adapts to sidebar width via ResponsiveContainer"
  artifacts:
    - path: "src/webview/app/components/TrendsTab.tsx"
      provides: "Trends tab with chart, period selector, and expandable data table"
      exports: ["TrendsTab"]
      min_lines: 80
    - path: "src/webview/app/components/UsageChart.tsx"
      provides: "Recharts stacked bar chart for token usage trends"
      exports: ["UsageChart"]
      min_lines: 50
    - path: "src/webview/app/components/SegmentedControl.tsx"
      provides: "Daily/Weekly/Monthly period selector"
      exports: ["SegmentedControl"]
      min_lines: 20
  key_links:
    - from: "src/webview/app/components/TrendsTab.tsx"
      to: "src/webview/app/components/UsageChart.tsx"
      via: "renders chart with trend data"
      pattern: "UsageChart.*data"
    - from: "src/webview/app/components/TrendsTab.tsx"
      to: "src/webview/app/components/SegmentedControl.tsx"
      via: "period selection state"
      pattern: "SegmentedControl.*onChange"
    - from: "src/webview/app/components/UsageChart.tsx"
      to: "recharts"
      via: "BarChart with stacked bars"
      pattern: "stackId.*tokens"
---

<objective>
Build the Trends tab with Recharts stacked bar chart, period selector (daily/weekly/monthly), custom tooltip, and expandable data table.

Purpose: The Trends tab answers "how is my usage trending over time?" -- showing historical patterns through stacked bar charts with breakdowns by token type and a switchable time granularity.

Output: Fully functional Trends tab with interactive Recharts chart, period switching, hover tooltips, and collapsible data table showing exact numbers.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webview-dashboard/05-RESEARCH.md
@.planning/phases/05-webview-dashboard/05-CONTEXT.md
@.planning/phases/05-webview-dashboard/05-02-SUMMARY.md

@src/webview/app/types.ts
@src/webview/app/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SegmentedControl and UsageChart components</name>
  <files>src/webview/app/components/SegmentedControl.tsx, src/webview/app/components/UsageChart.tsx</files>
  <action>
**SegmentedControl.tsx:**
A simple button group for period selection. Props:
- `options: { value: string; label: string }[]`
- `selected: string`
- `onChange: (value: string) => void`

Implementation:
- Render a row of buttons inside a `segmented-control` wrapper
- Selected button gets `segment-active` class, others get `segment-inactive`
- Use VS Code CSS variables for active/inactive styling (see app.css for tab patterns -- similar approach)
- Each button calls `onChange(option.value)` on click

Add CSS to app.css (or inline styles referencing CSS variables):
```css
.segmented-control {
  display: inline-flex;
  border: 1px solid var(--vscode-panel-border);
  border-radius: 4px;
  overflow: hidden;
}
.segment-active, .segment-inactive {
  padding: 4px 12px;
  border: none;
  cursor: pointer;
  font-family: var(--vscode-font-family);
  font-size: calc(var(--vscode-font-size) * 0.9);
}
.segment-active {
  background: var(--vscode-button-background);
  color: var(--vscode-button-foreground);
}
.segment-inactive {
  background: transparent;
  color: var(--vscode-foreground);
}
```

**UsageChart.tsx:**
Recharts stacked bar chart wrapper. Props:
- `data: TrendDataPoint[]` (array of { period, inputTokens, outputTokens, cacheCreationTokens, cacheReadTokens, totalCost })

Implementation:
1. Import from recharts: `BarChart, Bar, XAxis, YAxis, Tooltip, Legend, ResponsiveContainer`
2. Wrap in `<ResponsiveContainer width="100%" height={250}>` for sidebar-safe sizing
3. Create `<BarChart data={data}>` with:
   - `<XAxis dataKey="period" />` with VS Code foreground stroke color
   - `<YAxis />` with VS Code foreground stroke color, and a tick formatter that abbreviates tokens (1M, 500K, etc.)
   - `<Tooltip content={<CustomTooltip />} />` -- custom tooltip component (defined in same file)
   - `<Legend />` for token type labels
   - Four `<Bar>` components, all with `stackId="tokens"`:
     - `dataKey="inputTokens"` fill="#4FC3F7" name="Input"
     - `dataKey="outputTokens"` fill="#81C784" name="Output"
     - `dataKey="cacheCreationTokens"` fill="#FFB74D" name="Cache Write"
     - `dataKey="cacheReadTokens"` fill="#CE93D8" name="Cache Read"
4. CustomTooltip component:
   - Only renders when `active && payload`
   - Shows period label
   - Lists each token type with its color dot and formatted value
   - Shows total at bottom in bold
   - Styled with VS Code background/border CSS variables

Use the same token type colors defined as CSS custom properties in app.css from Plan 05-03. Reference them as string constants in the component (matching the CSS values).

Handle empty data gracefully: if data array is empty, show "No trend data available" message instead of empty chart.
  </action>
  <verify>
Run `npm run build` to confirm Recharts imports resolve and bundle correctly.
Run `npm run compile` to confirm TypeScript is happy with Recharts types.
  </verify>
  <done>
SegmentedControl renders button group with active/inactive states. UsageChart renders Recharts stacked bar chart with 4 token types, custom tooltip, and responsive sizing. Both compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build TrendsTab with chart, period selector, cost summary, and data table</name>
  <files>src/webview/app/components/TrendsTab.tsx</files>
  <action>
**TrendsTab.tsx:**
Props: `data: DashboardData | null`. If null, return null.

Layout:

**Top: Period selector + cost summary**
- `<SegmentedControl>` with options: Daily, Weekly, Monthly
- Use `useState<'daily' | 'weekly' | 'monthly'>('daily')` for selected period
- When period changes:
  1. Update local state
  2. Send message to extension: `vscode.postMessage({ type: 'changePeriod', period })`
  3. The chart should update to show the appropriate data

**Middle: Stacked bar chart**
- `<UsageChart data={filteredTrendData} />`
- `filteredTrendData` comes from `data.trendData` -- the extension sends the right data based on the active period
- For now, the component renders whatever is in `data.trendData` (the extension decides granularity)

**Cost summary row**
- Below the chart, a separate card/row showing cost breakdown:
  - "Total: $X.XX" for the visible period range
  - "Average/period: $X.XX" computed from the trend data
- Per CONTEXT.md: Cost gets its own separate card, not overlaid on the chart

**Bottom: Expandable data table**
- Collapsed by default (per CONTEXT.md decision)
- Click header "Show Details" / "Hide Details" to toggle
- Use `useState<boolean>(false)` for expanded state
- When expanded, show an HTML table with columns: Period | Input | Output | Cache Write | Cache Read | Total | Cost
- Each row from `data.trendData`
- Format tokens with commas, cost with $
- Total row at bottom
- Style table with VS Code CSS variables:
  - `--vscode-foreground` for text
  - `--vscode-panel-border` for borders
  - alternating row backgrounds optional (use `--vscode-list-hoverBackground` for alternate rows)

Persist period selection with `vscode.setState/getState` alongside the tab state.
  </action>
  <verify>
Run `npm run build` to confirm full bundle compiles including Recharts.
Run `npm run compile` for TypeScript check.
Verify TrendsTab imports and uses UsageChart and SegmentedControl.
  </verify>
  <done>
TrendsTab renders period selector, stacked bar chart, cost summary, and expandable data table. Period switching updates the chart. Data table is collapsed by default. All styled with VS Code theme.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (Recharts bundles correctly into webview.js)
2. `npm run compile` passes
3. UsageChart uses stackId for stacked bars
4. SegmentedControl has daily/weekly/monthly options
5. TrendsTab has chart, cost summary, and expandable data table
6. Chart uses ResponsiveContainer for sidebar width safety
7. Custom tooltip shows all token types + total
8. Empty data state handled gracefully
</verification>

<success_criteria>
Trends tab is fully functional with Recharts chart, period switching, cost summary card, and collapsible data table. Chart renders correctly with stacked bars and hover tooltips.
</success_criteria>

<output>
After completion, create `.planning/phases/05-webview-dashboard/05-04-SUMMARY.md`
</output>
