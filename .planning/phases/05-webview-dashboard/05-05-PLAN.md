---
phase: 05-webview-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - src/webview/app/components/SessionTab.tsx
  - src/webview/app/App.tsx
autonomous: true

must_haves:
  truths:
    - "Session tab shows current session tokens, duration, and message count"
    - "Session tab shows current session vs average comparison with visual bar"
    - "App.tsx renders all three real tab components (OverviewTab, TrendsTab, SessionTab)"
    - "When sessionCount < 2, comparison section shows 'Not enough sessions' message"
  artifacts:
    - path: "src/webview/app/components/SessionTab.tsx"
      provides: "Session detail view with comparison to average"
      exports: ["SessionTab"]
      min_lines: 60
    - path: "src/webview/app/App.tsx"
      provides: "Root component rendering all three tabs with real components"
      contains: "SessionTab"
  key_links:
    - from: "src/webview/app/App.tsx"
      to: "src/webview/app/components/SessionTab.tsx"
      via: "tab conditional rendering"
      pattern: "activeTab.*session.*SessionTab"
    - from: "src/webview/app/App.tsx"
      to: "src/webview/app/components/TrendsTab.tsx"
      via: "tab conditional rendering"
      pattern: "activeTab.*trends.*TrendsTab"
---

<objective>
Build the Session tab and wire all three real tab components into App.tsx.

Purpose: The Session tab completes the dashboard UI by showing session-level comparison data. Wiring all three tab components into App.tsx removes all placeholder content.

Output: SessionTab component with session comparison UI. App.tsx rendering OverviewTab, TrendsTab, and SessionTab with real data.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webview-dashboard/05-RESEARCH.md
@.planning/phases/05-webview-dashboard/05-CONTEXT.md
@.planning/phases/05-webview-dashboard/05-03-SUMMARY.md
@.planning/phases/05-webview-dashboard/05-04-SUMMARY.md

@src/webview/app/types.ts
@src/webview/app/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Session tab with session comparison</name>
  <files>src/webview/app/components/SessionTab.tsx</files>
  <action>
**SessionTab.tsx:**
Props: `data: DashboardData | null`. If null, return null.

Layout:

**Section 1: Current Session Summary**
- Card showing current active session stats
- Key metrics in a grid: Session Tokens (output), Session Duration, Messages Sent
- If no active session (sessionTokens = 0 or windowStart null): show "No active session detected"

**Section 2: Session vs Average Comparison**
- Shows how current session compares to average
- Two-column layout or comparison bars:
  - "This Session: X tokens" vs "Average Session: Y tokens"
  - Visual comparison bar (like a horizontal bar chart with two bars)
  - Percentage: "XX% of average" or "XX% above/below average"
- Use `data.currentSessionTokens` and `data.averageSessionTokens`
- If `data.sessionCount` < 2, show "Not enough sessions for comparison" instead

**Section 3: Session History Insights**
- "Total sessions tracked: N"
- "Average session length: Xh Ym" (computed from averageSessionTokens / typical burn rate, or just show token count)
- Brief context: "Based on N sessions"

Format tokens, costs, and times using inline formatting helpers (same approach as OverviewTab -- no dependency on src/ui/formatting.ts).
  </action>
  <verify>
Run `npm run build` to confirm bundle compiles.
Run `npm run compile` for TypeScript check.
  </verify>
  <done>
SessionTab renders current session summary, comparison vs average, and session history insights.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire all three tab components into App.tsx</name>
  <files>src/webview/app/App.tsx</files>
  <action>
Update App.tsx to replace all placeholder tab content with real components:

1. Import SessionTab: `import { SessionTab } from './components/SessionTab';`
2. Import TrendsTab: `import { TrendsTab } from './components/TrendsTab';`
3. Replace the placeholder div `"Session tab (Plan 05-05)"` with `<SessionTab data={data} />`
4. Replace the placeholder div `"Trends tab (Plan 05-04)"` with `<TrendsTab data={data} />`
5. Verify OverviewTab import and rendering are already in place from Plan 05-03.

After this change, all three tabs render real components. No placeholder text remains.
  </action>
  <verify>
Run `npm run build` to confirm bundle compiles.
Run `npm run compile` for TypeScript check.
Verify App.tsx now imports and renders OverviewTab, TrendsTab, and SessionTab.
  </verify>
  <done>
App.tsx renders all three real tab components with data props. No placeholder content remains.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (both bundles)
2. `npm run compile` passes (zero errors)
3. App.tsx imports and renders OverviewTab, TrendsTab, SessionTab
4. SessionTab shows session summary, comparison bars, and history insights
5. SessionTab handles edge case of < 2 sessions gracefully
</verification>

<success_criteria>
Session tab is fully functional with session comparison UI. App.tsx renders all three real tab components. No placeholder content remains in the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/05-webview-dashboard/05-05-SUMMARY.md`
</output>
