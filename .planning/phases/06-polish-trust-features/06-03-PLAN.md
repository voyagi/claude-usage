---
phase: 06-polish-trust-features
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README clearly states zero network calls and fully local operation"
    - "README includes a permissions table showing what the extension reads and what it does NOT do"
    - "README provides installation and usage instructions"
    - "README serves as a marketplace differentiator emphasizing trust"
  artifacts:
    - path: "README.md"
      provides: "Trust-focused documentation for marketplace listing"
      contains: "Zero Network Calls"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "Feature list matches registered commands"
      pattern: "Command Palette"
---

<objective>
Create a trust-focused README.md that serves as both project documentation and VS Code marketplace listing content.

Purpose: The README is the extension's first impression on the marketplace. It must immediately communicate the trust differentiator ("here's exactly what we do and DON'T do") and provide clear usage instructions. This directly fulfills success criterion 6: "Documentation clearly states zero network calls, fully local."
Output: README.md at project root.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-trust-features/06-CONTEXT.md

@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trust-focused README.md</name>
  <files>README.md</files>
  <action>
Create `README.md` at project root with the following structure and content. This should read as a marketplace listing that builds trust immediately.

**Structure:**

```markdown
# Claude Usage Monitor

Local-only usage monitoring for Claude Code. Zero network calls. Zero telemetry. Your data stays on your machine.

## Why This Extension?

Existing Claude usage trackers require API keys, make network calls, or send telemetry. This extension takes a different approach:

- **Reads local files only** -- parses JSONL session logs from `~/.claude/projects/`
- **Zero network calls** -- no outbound requests, ever
- **No API keys needed** -- works entirely from local data
- **Minimal dependencies** -- small, auditable codebase

## What You Get

- **Always-on status bar** showing tokens, cost, and rate limit proximity
- **Sidebar dashboard** with token breakdown, trend charts, and session comparison
- **Rate limit tracking** with burn rate calculation and proximity warnings
- **Auto-detection** of your Claude plan tier from local credentials
- **Data export** to JSON for custom analysis

## What This Extension Accesses

Full transparency -- here is exactly what this extension reads and stores:

| Action | Details |
|--------|---------|
| **Reads** | `~/.claude/projects/**/*.jsonl` (session logs) |
| **Reads** | `~/.claude/.credentials.json` (plan tier auto-detection) |
| **Stores** | VS Code `globalState` (cached aggregations, local only) |

### What This Extension Does NOT Do

| Never | Explanation |
|-------|-------------|
| Network requests | No HTTP, WebSocket, or any outbound connections |
| Telemetry | No usage tracking, analytics, or crash reporting |
| Data transmission | No data leaves your machine, period |
| API key access | No Anthropic API keys needed or requested |
| File modification | Only reads Claude session files, never writes to them |
| Workspace access | Does not read your project source code |

## Installation

1. Install from the VS Code marketplace (or `code --install-extension voyagi.claude-usage-monitor`)
2. The extension activates automatically when `~/.claude/` exists
3. Look for the Claude Usage icon in the activity bar

## Features

### Status Bar

Always-visible metrics showing:
- Token count and estimated cost
- Rate limit proximity with color coding (green/yellow/red)
- Burn rate and cooldown timer
- Click either item to open the dashboard

### Dashboard (Sidebar Panel)

- **Overview tab**: Token breakdown, rate limits with progress bars, session timing, burn rate
- **Trends tab**: Stacked bar charts for daily/weekly/monthly usage, expandable data table
- **Session tab**: Current session vs. historical average comparison

### Command Palette

All commands are available under `Claude Usage:` in the command palette:

| Command | Description |
|---------|-------------|
| Refresh Usage Data | Reparse all session files |
| Switch Plan Tier | Change between Pro, Max 5x, Max 20x |
| Export Usage Data | Save all usage data as JSON |
| Open Dashboard | Focus the sidebar dashboard |
| Toggle Status Bar | Show/hide status bar items |
| Show Data Source Path | Display the watched directory |
| Open Settings | Jump to extension settings |
| Reset Rate Limit Estimates | Clear learned rate limits |
| Reset Session Tracking | Clear all cached data and reparse |

### Data Export

Export all usage data to JSON with:
- **Summary**: Human-friendly totals (tokens, cost, session count)
- **Raw**: Complete time bucket data for custom analysis

### Configuration

Configure via VS Code Settings (`Ctrl+,` then search "Claude Usage"):

| Setting | Default | Description |
|---------|---------|-------------|
| Plan Type | `max5` | Your Claude plan (pro, max5, max20) |
| Refresh Interval | `60` | Seconds between usage checks |
| Compact Mode | `false` | Shorter status bar text |
| Warning Thresholds | 60% / 95% | Yellow and red warning levels |
| Burn Rate Window | `15` | Minutes for burn rate calculation |
| Pricing Overrides | `{}` | Custom per-model pricing |
| Rate Limit Overrides | `0` | Manual token limit overrides |

## How It Works

1. Claude Code writes session data as JSONL files to `~/.claude/projects/`
2. This extension watches those files for changes (500ms debounce)
3. New records are parsed incrementally (byte offset tracking)
4. Usage is aggregated into time buckets (session, daily, weekly, monthly)
5. Status bar and dashboard update in real-time
6. All aggregated data is cached in VS Code globalState for instant startup

## Supported Plans

| Plan | Status |
|------|--------|
| Pro ($20/mo) | Supported |
| Max 5x ($100/mo) | Supported (default) |
| Max 20x ($200/mo) | Supported |
| Free | Partial (no rate limit tracking) |

Plan is auto-detected from `~/.claude/.credentials.json` with manual override available.

## Requirements

- VS Code 1.96.0 or later
- Claude Code installed (creates `~/.claude/` directory)
- No additional setup needed

## License

MIT
```

**Important notes:**
- Do NOT include emoji in the README (per user instructions).
- Keep the permissions tables as the centerpiece -- this is the trust differentiator.
- The "What This Extension Does NOT Do" table should be prominent and easy to scan.
- Match command names exactly to what's registered in package.json.
- Verify settings table matches actual package.json configuration properties.
  </action>
  <verify>
Verify README.md exists: `ls README.md`
Verify no emoji in file (the lock emoji is only in the webview components, not README).
Verify all command names match package.json: cross-reference the command palette table against package.json contributes.commands.
Verify all settings match package.json: cross-reference settings table against contributes.configuration.properties.
Run a quick markdownlint check if available, or visually verify:
- Blank lines around headings (MD022)
- Blank lines around lists (MD032)
- Proper table pipe spacing (MD060)
  </verify>
  <done>
README.md exists at project root with: trust-first messaging, "What This Extension Accesses" and "Does NOT Do" tables, complete command palette reference, configuration reference, architecture overview. No emoji. Markdown properly formatted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final integration verification</name>
  <files></files>
  <action>
Run full verification suite to confirm Phase 6 is complete:

1. `npm run compile` -- TypeScript compilation clean
2. `npm run build` -- Both bundles succeed (extension + webview)
3. `npm test` -- All existing tests pass
4. Verify all 6 success criteria from the roadmap:
   - SC1: Command palette has refresh, plan selection, export (grep package.json)
   - SC2: Export command exists (grep extension.ts for exportData)
   - SC3: Data source path in dashboard footer (grep App.tsx for dataSourcePath)
   - SC4: refreshInterval, warning thresholds, pricing overrides in config (grep package.json)
   - SC5: Activation guard for ~/.claude/ (grep extension.ts for .claude)
   - SC6: README states zero network calls (grep README.md)

This task is a verification-only task -- no code changes. If any check fails, fix the issue in the relevant file (likely a typo or missing import).
  </action>
  <verify>
All 6 success criteria verified by grep/inspection.
npm run compile exits 0.
npm run build exits 0.
npm test exits 0.
  </verify>
  <done>
Phase 6 fully verified: all commands registered, export works, data source visible, settings complete, activation guarded, documentation states zero network calls.
  </done>
</task>

</tasks>

<verification>
1. README.md exists at project root
2. README contains "Zero Network Calls" prominently
3. README contains "What This Extension Does NOT Do" table
4. README command table matches package.json commands
5. README settings table matches package.json configuration
6. Full build pipeline passes (compile + build + test)
7. All 6 phase success criteria verified
</verification>

<success_criteria>
- README.md exists and communicates trust as primary differentiator
- Permissions tables clearly list what IS and IS NOT accessed
- All cross-references between README and package.json are accurate
- Build pipeline clean (compile + build + test all pass)
- All 6 roadmap success criteria for Phase 6 verified
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-trust-features/06-03-SUMMARY.md`
</output>
