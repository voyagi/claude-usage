---
phase: 03-basic-ui-status-bar
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/extension.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "Extension activates and shows two status bar items with real data"
    - "SessionWatcher updates flow through StatusBarManager to render live usage"
    - "Clicking status bar opens quick pick menu with working actions"
    - "Refresh command reparses all JSONL files and updates display"
    - "Switch Plan command changes plan tier in settings"
    - "Reset Session command clears data and reparses"
    - "Compact mode setting toggles abbreviated display"
    - "Extension compiles and bundles without errors"
    - "Status bar cooldown item shows reset countdown when worst rate limit >= 60%"
  artifacts:
    - path: "src/extension.ts"
      provides: "Refactored entry point using StatusBarManager, new command registrations"
      exports: ["activate", "deactivate"]
    - path: "package.json"
      provides: "Registered commands and compactMode setting"
      contains: "claude-usage.showMenu"
  key_links:
    - from: "src/extension.ts"
      to: "src/ui/statusBar.ts"
      via: "creates StatusBarManager instance, calls update() in onUpdate callback"
      pattern: "new StatusBarManager"
    - from: "src/extension.ts"
      to: "src/core/rateLimits.ts"
      via: "calls buildStatusBarData to transform TimeBuckets into StatusBarData"
      pattern: "buildStatusBarData"
    - from: "src/extension.ts"
      to: "src/ui/quickPick.ts"
      via: "registers showMenu command that calls showUsageMenu"
      pattern: "showUsageMenu"
    - from: "src/extension.ts"
      to: "src/ui/formatting.ts"
      via: "imports formatTokens (replacing local copy removed during refactor)"
      pattern: "import.*from.*formatting"
    - from: "package.json"
      to: "src/extension.ts"
      via: "contributes.commands defines commands that extension.ts registers handlers for"
      pattern: "claude-usage\\.showMenu"
---

<objective>
Wire StatusBarManager and all new commands into the extension entry point, replacing the old single-item status bar code. Update package.json with new commands and settings.

Purpose: This is the integration plan that brings Plans 01 and 02 together into a working extension. The old status bar code in extension.ts gets replaced with StatusBarManager. New commands get registered for the quick pick menu actions. package.json declares all new commands and the compactMode setting.

Output: Refactored extension.ts using StatusBarManager, updated package.json with 4 new commands and compactMode setting, working end-to-end status bar UI.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-basic-ui-status-bar/03-CONTEXT.md
@.planning/phases/03-basic-ui-status-bar/03-RESEARCH.md
@.planning/phases/03-basic-ui-status-bar/03-01-SUMMARY.md
@.planning/phases/03-basic-ui-status-bar/03-02-SUMMARY.md
@src/extension.ts
@src/ui/statusBar.ts
@src/ui/quickPick.ts
@src/core/rateLimits.ts
@src/ui/formatting.ts
@src/watcher/sessionWatcher.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json with new commands and settings</name>
  <files>package.json</files>
  <action>
  Update package.json contributes section:

  **Commands** — add these to the existing commands array (keep claude-usage.clearData):
  1. `{ "command": "claude-usage.showMenu", "title": "Claude Usage: Show Menu" }`
  2. `{ "command": "claude-usage.refresh", "title": "Claude Usage: Refresh Data" }`
  3. `{ "command": "claude-usage.switchPlan", "title": "Claude Usage: Switch Plan Tier" }`
  4. `{ "command": "claude-usage.viewSummary", "title": "Claude Usage: View Usage Summary" }`
  5. `{ "command": "claude-usage.resetSession", "title": "Claude Usage: Reset Session Tracking" }`

  **Settings** — add to existing configuration.properties (keep planType and pricing):
  - `"claude-usage.compactMode"`: type boolean, default false, description "Use compact status bar display (shorter text for narrow windows)"

  Do NOT remove existing commands or settings. This is additive only.
  </action>
  <verify>Validate package.json is valid JSON (use `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` or similar). Confirm all 6 commands are present. Confirm compactMode setting exists.</verify>
  <done>package.json has 6 commands (clearData, showMenu, refresh, switchPlan, viewSummary, resetSession) and 4 settings (planType, pricing, compactMode). Valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor extension.ts to use StatusBarManager</name>
  <files>src/extension.ts</files>
  <action>
  Refactor extension.ts to replace the old single StatusBarItem with StatusBarManager and register all new commands.

  NOTE: This task has borderline complexity (many changes to one file). If context pressure builds during execution, focus on: (1) StatusBarManager wiring, (2) command registration, (3) cleanup of old code — in that priority order. Leave cosmetic cleanups for a follow-up if needed.

  **Remove:**
  - The old `vscode.window.createStatusBarItem(...)` call and statusBarItem variable
  - The old `updateStatusBar()` function (replaced by StatusBarManager.update)
  - The local `formatTokens()` function (moved to src/ui/formatting.ts in Plan 01)
  - The `getSelectedPlan()` function (inline where needed, or keep as helper)

  **Add imports:**
  ```typescript
  import { StatusBarManager } from './ui/statusBar.js';
  import { showUsageMenu, showPlanPicker } from './ui/quickPick.js';
  import { buildStatusBarData } from './core/rateLimits.js';
  import { formatTokens } from './ui/formatting.js';
  import type { PlanType } from './types.js';
  ```

  **In activate():**

  1. Create StatusBarManager: `const statusBar = new StatusBarManager(context);`

  2. Read plan type helper (keep inline):
     ```typescript
     function getSelectedPlan(): PlanType {
       const config = vscode.workspace.getConfiguration('claude-usage');
       return config.get<PlanType>('planType', 'max5');
     }
     ```

  3. Create SessionWatcher with updated onUpdate callback:
     ```typescript
     sessionWatcher = new SessionWatcher(context, (buckets, stats) => {
       const data = buildStatusBarData(buckets, stats, getSelectedPlan());
       statusBar.update(data);
       store.saveUsageData(buckets, stats).catch((err) => {
         logger.error(`Failed to save usage data: ${err.message}`, err instanceof Error ? err : undefined);
       });
     });
     ```

  4. Register commands (all pushed to context.subscriptions):

     **showMenu:** `vscode.commands.registerCommand('claude-usage.showMenu', () => showUsageMenu())`

     **refresh:** `vscode.commands.registerCommand('claude-usage.refresh', async () => {`
       - Call `statusBar.showRefreshing()`
       - Call `await performInitialParse(store, statusBar, sessionWatcher!)`
       - If error, call `statusBar.showError(err.message)`
     `})`

     **switchPlan:** `vscode.commands.registerCommand('claude-usage.switchPlan', async () => {`
       - Call `const plan = await showPlanPicker()`
       - If plan selected: update config via `vscode.workspace.getConfiguration('claude-usage').update('planType', plan, vscode.ConfigurationTarget.Global)`
       - Show info message: `Plan changed to ${plan}. Refreshing...`
       - Trigger refresh command
     `})`

     **viewSummary:** `vscode.commands.registerCommand('claude-usage.viewSummary', () => {`
       - For now: `vscode.window.showInformationMessage('Usage summary dashboard coming in Phase 5.')`
     `})`

     **resetSession:** `vscode.commands.registerCommand('claude-usage.resetSession', async () => {`
       - Confirm with user: `vscode.window.showWarningMessage('Reset all session tracking data?', 'Yes', 'No')`
       - If 'Yes': call existing clearData logic (clear store, reset watcher, show no data)
       - Trigger full reparse
     `})`

     **clearData:** Keep existing clearData command, but update it to call `statusBar.showNoData()` instead of setting statusBarItem.text directly.

  5. Update `performInitialParse` signature to accept StatusBarManager instead of StatusBarItem:
     ```typescript
     async function performInitialParse(
       store: UsageStore,
       statusBar: StatusBarManager,
       watcher: SessionWatcher
     ): Promise<void>
     ```
     - Replace all `statusBarItem.text = ...` with `statusBar.showNoData()` or `statusBar.update(data)`
     - When loading cached data: call `buildStatusBarData()` then `statusBar.update(data)`
     - When full parse completes: call `buildStatusBarData()` then `statusBar.update(data)`
     - On error: call `statusBar.showError(err.message)`

  6. Listen for configuration changes to update compact mode:
     ```typescript
     context.subscriptions.push(
       vscode.workspace.onDidChangeConfiguration((e) => {
         if (e.affectsConfiguration('claude-usage')) {
           // Re-render status bar with current data
           // StatusBarManager will re-read compactMode setting internally
         }
       })
     );
     ```

  **Key constraints:**
  - Keep the `format` import from date-fns only if still needed (check if any code in extension.ts still uses it after refactor — likely not, remove if unused)
  - Keep Logger, UsageStore, SessionWatcher imports
  - Remove `getTimeBucketSummary` import if no longer used in extension.ts (buildStatusBarData handles it internally)
  - The onUpdate callback must call buildStatusBarData to transform TimeBuckets into StatusBarData before passing to statusBar.update()
  </action>
  <verify>
  1. Run `npx tsc --noEmit` (via compile.ps1) — no type errors
  2. Run `node esbuild.config.mjs` (via build.ps1) — bundle builds successfully
  3. Grep extension.ts for "createStatusBarItem" — should NOT appear (old code removed)
  4. Grep extension.ts for "StatusBarManager" — should appear (new code wired)
  5. Grep extension.ts for "showUsageMenu" — should appear (command registered)
  6. Grep extension.ts for "buildStatusBarData" — should appear (data transformation)
  7. Grep extension.ts for "from.*formatting" — should appear (formatting import)
  </verify>
  <done>extension.ts uses StatusBarManager for all status bar rendering, registers 5 new commands (showMenu, refresh, switchPlan, viewSummary, resetSession), onUpdate callback transforms TimeBuckets via buildStatusBarData, performInitialParse uses StatusBarManager API, imports formatTokens from ui/formatting.ts (local copy removed). Old single-item code fully replaced. Extension compiles and bundles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `node esbuild.config.mjs` produces dist/extension.js without errors
- package.json is valid JSON with 6 commands and compactMode setting
- extension.ts imports from ui/statusBar, ui/quickPick, core/rateLimits, ui/formatting
- No remnants of old status bar code (no bare createStatusBarItem, no local formatTokens, no local updateStatusBar)
- All commands are both declared in package.json and registered in extension.ts
- Status bar cooldown item shows reset countdown when worst rate limit >= 60% (SB-04 end-to-end: calculation from Plan 01 rateLimits.ts flows through Plan 02 StatusBarManager cooldownItem, wired together here in extension.ts onUpdate callback)
</verification>

<success_criteria>
1. Extension compiles and bundles without errors
2. StatusBarManager replaces old single-item status bar
3. All 5 new commands registered and connected to handlers
4. SessionWatcher onUpdate flows through buildStatusBarData -> StatusBarManager.update()
5. Quick pick menu opens on status bar click
6. Switch Plan updates config and refreshes display
7. Refresh command reparses with spinner feedback
8. Reset command clears data with confirmation dialog
9. compactMode setting declared and read by StatusBarManager
10. Cooldown timer displays correctly when rate limits are elevated (>= 60%), confirming Plan 01 calculation + Plan 02 display work end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-basic-ui-status-bar/03-03-SUMMARY.md`
</output>
