---
phase: 04-rate-limiting-burn-rate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/rateLimits.ts
  - src/ui/formatting.ts
autonomous: true

must_haves:
  truths:
    - "Rate limit calculation uses urgency-weighted scoring for color selection"
    - "Session reset time correctly computed as oldestSessionFirstMessage + 5 hours"
    - "Formatting module can format time-until-limit as human-readable string"
    - "worstPercentage still drives threshold comparison but urgency score determines which limit is most critical"
  artifacts:
    - path: "src/core/rateLimits.ts"
      provides: "Urgency-weighted rate limit calculation, proper session reset times"
      exports: ["calculateRateLimits", "calculateBurnRate", "buildStatusBarData", "calculateUrgencyScore"]
      min_lines: 100
    - path: "src/ui/formatting.ts"
      provides: "Extended formatting with time-until-limit"
      exports: ["formatTokens", "formatTokensExact", "formatCooldown", "formatCost", "formatPercentage", "formatBurnRate", "formatTimeUntilLimit"]
      min_lines: 80
  key_links:
    - from: "src/core/rateLimits.ts"
      to: "calculateUrgencyScore"
      via: "exported function used by statusBar.ts in Plan 04"
      pattern: "export function calculateUrgencyScore"
    - from: "src/ui/formatting.ts"
      to: "statusBar.ts"
      via: "formatTimeUntilLimit imported by status bar"
      pattern: "export function formatTimeUntilLimit"
---

<objective>
Enhance the existing rate limit calculation engine with urgency-weighted scoring and fix session reset time calculation. Add time-until-limit formatting to the display module.

Purpose: Current rateLimits.ts has a broken session reset time calculation and no urgency weighting. Current formatting.ts lacks time-until-limit display. These are prerequisites for the enhanced status bar display in Plan 04-04.

Output: Enhanced `rateLimits.ts` with proper urgency scoring and `formatting.ts` with new format function.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types.ts
@src/core/rateLimits.ts
@src/ui/formatting.ts
@.planning/phases/04-rate-limiting-burn-rate/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance rateLimits.ts with urgency weighting and proper reset times</name>
  <files>src/core/rateLimits.ts</files>
  <action>
    Modify the existing `calculateRateLimits` function and add urgency scoring:

    1. **Fix session reset time calculation** (currently broken):
       Replace the awkward `addDays(oldestSessionTime, 0).setHours(...)` pattern with:
       ```typescript
       resetTime: oldestSessionTime ? addHours(oldestSessionTime, 5) : null
       ```
       Import `addHours` from date-fns. Remove the separate "fix resetTime" block below.

    2. **Add `calculateUrgencyScore` exported function:**
       ```typescript
       export function calculateUrgencyScore(limit: RateLimitInfo, now: Date): number
       ```
       Formula: `percentage * (1 / sqrt(max(1, hoursUntilReset)))` where hoursUntilReset = `differenceInHours(limit.resetTime, now)`.
       Returns 0 if limit.percentage is 0 or limit.resetTime is null.
       Import `differenceInHours` from date-fns.

    3. **Update `buildStatusBarData`** to accept an optional `burnRateOverride?: number` parameter.
       If provided, use it instead of the simple `calculateBurnRate(buckets)`. This will let extension.ts pass in the EMA-smoothed burn rate from Plan 01.
       Signature: `buildStatusBarData(buckets, stats, planType, burnRateOverride?)`.

    4. **Keep existing `calculateBurnRate`** as-is (simple 10-min lookback). It remains as the fallback when no EMA tracker is available. Plan 04-04 will choose which burn rate to use.

    5. Do NOT change the color threshold logic (that stays in statusBar.ts). Urgency scoring is exported for statusBar.ts to use.
  </action>
  <verify>
    `npm run compile` passes.
    Verify `addHours` import exists.
    Verify `calculateUrgencyScore` is exported.
    Verify `buildStatusBarData` has optional `burnRateOverride` parameter.
    Verify old resetTime hack is removed.
  </verify>
  <done>
    - Session reset time uses `addHours(oldestSessionTime, 5)` correctly
    - `calculateUrgencyScore` exported and computes `pct * (1/sqrt(hoursUntilReset))`
    - `buildStatusBarData` accepts optional burn rate override
    - Existing calculateBurnRate unchanged (backwards compatible)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add formatTimeUntilLimit to formatting.ts</name>
  <files>src/ui/formatting.ts</files>
  <action>
    Add a new exported function to the existing formatting module:

    ```typescript
    /**
     * Format estimated time until hitting a rate limit
     * null: '' (idle, can't predict)
     * 0: 'LIMIT HIT'
     * < 1 min: '<1m at current pace'
     * < 60 min: 'Xm at current pace' (e.g. '45m at current pace')
     * >= 60 min: 'Xh Ym at current pace' (e.g. '2h 15m at current pace')
     */
    export function formatTimeUntilLimit(minutes: number | null): string
    ```

    Implementation:
    - `null` -> `''`
    - `0` -> `'LIMIT HIT'`
    - `< 1` -> `'<1m at current pace'`
    - `< 60` -> `'${Math.round(minutes)}m at current pace'`
    - `>= 60` -> `'${hours}h ${mins}m at current pace'`

    This is a pure function with no side effects. No new imports needed.
  </action>
  <verify>
    `npm run compile` passes.
    Verify function is exported from formatting.ts.
    Manual check: `formatTimeUntilLimit(null)` = `''`, `formatTimeUntilLimit(0)` = `'LIMIT HIT'`, `formatTimeUntilLimit(45)` = `'45m at current pace'`, `formatTimeUntilLimit(135)` = `'2h 15m at current pace'`.
  </verify>
  <done>
    - `formatTimeUntilLimit` is exported from formatting.ts
    - Handles null (idle), 0 (hit), <1min, minutes, hours+minutes
    - Includes "at current pace" suffix for user clarity
    - No new dependencies
  </done>
</task>

</tasks>

<verification>
1. `npm run compile` passes with no TypeScript errors
2. `npm run build` produces clean esbuild output
3. All existing code that imports from rateLimits.ts and formatting.ts continues to work (backwards compatible)
4. `calculateUrgencyScore` is a new export, doesn't break existing consumers
5. `buildStatusBarData` signature change is backwards compatible (optional param)
</verification>

<success_criteria>
- Session reset time correctly computed as `addHours(firstMessage, 5)`
- Urgency score exported and uses `percentage * 1/sqrt(hoursUntilReset)` formula
- `formatTimeUntilLimit` handles all edge cases (null, 0, minutes, hours)
- Both files compile cleanly and remain pure (no VS Code dependencies in formatting.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/04-rate-limiting-burn-rate/04-02-SUMMARY.md`
</output>
